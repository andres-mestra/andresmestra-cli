import { container } from '../../styles/components/editor/dashboard.module.scss'
const Users = () => {
  return <section className={container}>Users</section>
}

Users.layout = 'ADMIN'
export default Users
